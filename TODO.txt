
Page支持同步调用

Observer dom tree
Server Side Render
Directive return Render result {attrs, children}
&{}
~ translation
live:inner-html
live:inner-text
Comment style directive <!--\ld...--><!--/ld-->
ignore template tag
gulp pump, catch error
typescript report error

Custom element的支持, 专门的设置接口setLiveAttribute(name, value)

Todo List, Task(content, time), Add/Modify/Remove

live:disable指令, 停止内部渲染
live:inner/content/text/inner-text
live:inner-html指令, 把值做为标签内容
<live:each>
<live:if>

input value属性, 如果数据有变化, 则更新input.value, 否则忽略, (input value: attr 是原始的, prop 是实时的 )
如果新的attr和原来的不一样, 则表示要更新(同时更新attr和prop), 否则不变, (但如果的确要更新回attr呢?)

live:value-ref="field" for model value
$value="field.path" for model value
$checked="field.path"

attrInfo.isLastUpdate(dataManager) or dataManager.isLastUpdate(attrInfo)

